@model IEnumerable<FlightSystem2.Models.FlightBooking>

@{
    ViewBag.Title = "Index";
}

<h2>Booking History</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @*@Html.DisplayNameFor(model => model.Customer.username)*@Usename
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.Flight.flightName)*@Airline
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.Flight.flightName)*@Price
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.username)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Flight.flightName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Flight.price)
            </td>
        </tr>
    }

</table>
<div>
    @Html.ActionLink("Main Page", "MainUserPage", "Login")
</div>
